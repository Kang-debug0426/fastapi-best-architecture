# 12天学习进度管理表

> 项目：fastapi-best-architecture
> 开始日期：___________
> 目标：系统掌握 FastAPI 全栈工程实践

---

## Day 1：环境拉起 + 项目全貌

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 成功启动项目所有服务（Docker Compose 一键拉起）
- 能访问 Swagger UI 并看到所有接口
- 理解项目整体目录结构和各层职责

### 操作 Checklist
- [ ] 克隆仓库，阅读 README
- [ ] 安装 Docker Desktop，确认 docker --version 和 docker compose --version
- [ ] 执行 docker compose up -d，观察各容器启动顺序
- [ ] 访问 http://localhost:8000/docs，浏览 Swagger UI
- [ ] 访问 http://localhost:5555（Flower），确认 Celery Worker 在线
- [ ] 执行 docker compose ps，记录所有服务名称和端口
- [ ] 阅读 docker-compose.yml，画出服务依赖关系图
- [ ] 浏览项目目录，识别 backend/app/ 下各子目录用途

### 底层知识打卡
- [ ] **Docker**：镜像 vs 容器的区别；docker run 和 docker compose up 的关系
- [ ] **Docker Compose**：depends_on 的作用；networks 配置的意义
- [ ] **Linux**：docker logs <container> 查看日志；docker exec -it <container> bash 进入容器
- [ ] **FastAPI**：ASGI vs WSGI 的区别；uvicorn 的作用

### 理解验证
1. 用自己的话解释：Docker 容器和虚拟机有什么本质区别？
2. docker compose up 执行时，各服务的启动顺序是什么？为什么？
3. 项目中 backend/app/ 目录下，api/、crud/、model/、schema/ 各自负责什么？
4. Swagger UI 是怎么自动生成的？FastAPI 做了什么？

---

## Day 2：项目结构与路由分发

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 理解 FastAPI 路由注册机制和 APIRouter 的使用
- 能追踪一个 HTTP 请求从入口到响应的完整链路
- 理解项目分层架构（Router → Service → CRUD → DB）

### 操作 Checklist
- [ ] 找到主入口文件（main.py 或 app.py），阅读应用初始化代码
- [ ] 找到路由注册位置，理解 include_router 的用法
- [ ] 选一个具体接口（如用户登录），追踪完整调用链路
- [ ] 画出请求流转图：Client → Router → Service → CRUD → DB → Response
- [ ] 阅读 api/v1/ 目录，理解版本化路由设计
- [ ] 在 Swagger UI 中手动调用一个 GET 接口，观察请求/响应
- [ ] 找到 prefix、tags 的配置位置，理解其作用

### 底层知识打卡
- [ ] **FastAPI**：APIRouter 的作用；路由装饰器（@router.get）的工作原理
- [ ] **Python**：装饰器（decorator）的本质；函数作为一等公民
- [ ] **HTTP**：RESTful 设计规范；状态码语义（200/201/400/401/403/404/422/500）
- [ ] **Linux**：curl 命令发送 HTTP 请求；jq 格式化 JSON 输出

### 理解验证
1. APIRouter 和直接在 app 上注册路由有什么区别？为什么要用 APIRouter？
2. 请求进来后，FastAPI 如何知道调用哪个函数处理？
3. 项目为什么要分 api/、service/、crud/ 三层？各层的职责边界是什么？
4. HTTP 422 错误是什么情况下触发的？

---

## Day 3：Pydantic 数据校验 + 响应模型

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 理解 Pydantic v2 的数据校验机制
- 能读懂项目中所有 Schema 定义
- 理解请求体校验和响应模型过滤的工作原理

### 操作 Checklist
- [ ] 找到 schema/ 目录，阅读几个典型的 Schema 文件
- [ ] 理解 BaseModel、model_validator、field_validator 的用法
- [ ] 找到接口中 response_model 的使用，理解其过滤作用
- [ ] 故意发送错误格式的请求，观察 422 响应的结构
- [ ] 找到项目中 Optional、Annotated、Field 的使用示例
- [ ] 理解 model_dump() 和 model_validate() 的使用场景
- [ ] 阅读请求 Schema 和响应 Schema 的分离设计

### 底层知识打卡
- [ ] **Pydantic**：数据验证 vs 数据序列化；model_config 的作用
- [ ] **Python**：类型注解（Type Hints）；Optional[T] 等价于 T | None
- [ ] **FastAPI**：请求体（Body）、查询参数（Query）、路径参数（Path）的区别
- [ ] **JSON Schema**：Pydantic 如何生成 OpenAPI 文档

### 理解验证
1. Pydantic 的 BaseModel 和普通 Python 类有什么区别？
2. response_model 的作用是什么？不加会有什么风险？
3. 为什么要把请求 Schema（CreateXxx）和响应 Schema（GetXxx）分开定义？
4. model_validator(mode=before) 和 mode=after 有什么区别？

---

## Day 4：依赖注入（Depends）

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 深入理解 FastAPI 依赖注入系统
- 能读懂项目中所有 Depends 的使用
- 理解依赖注入在权限校验、数据库会话管理中的应用

### 操作 Checklist
- [ ] 找到项目中所有 Depends 的使用位置
- [ ] 理解数据库 Session 是如何通过依赖注入传递的
- [ ] 找到当前用户获取的依赖函数，理解其工作原理
- [ ] 理解依赖的嵌套（依赖中调用依赖）
- [ ] 找到 yield 依赖的使用，理解其生命周期
- [ ] 阅读全局依赖（app 级别）和路由级别依赖的区别
- [ ] 尝试自己写一个简单的依赖函数

### 底层知识打卡
- [ ] **FastAPI**：依赖注入 vs 手动传参；依赖的缓存机制（use_cache）
- [ ] **Python**：生成器（generator）和 yield 的工作原理
- [ ] **设计模式**：依赖倒置原则（DIP）；控制反转（IoC）
- [ ] **数据库**：数据库连接池的概念；Session 的生命周期管理

### 理解验证
1. FastAPI 的依赖注入和 Spring 的 @Autowired 有什么相似之处？
2. 为什么数据库 Session 要用 yield 依赖而不是普通依赖？
3. 依赖注入如何实现权限校验的复用？
4. 如果一个接口有多个依赖都需要同一个 Session，FastAPI 会创建几个 Session？

---

## Day 5：SQLAlchemy ORM + CRUD 层

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 理解 SQLAlchemy 2.0 异步 ORM 的使用方式
- 能读懂项目中所有 Model 定义和 CRUD 操作
- 理解数据库迁移（Alembic）的工作流程

### 操作 Checklist
- [ ] 找到 model/ 目录，阅读几个典型的 Model 定义
- [ ] 理解 Column 类型、关系映射（relationship）的写法
- [ ] 找到 crud/ 目录，阅读 CRUD 基类的实现
- [ ] 理解异步 Session（AsyncSession）的使用方式
- [ ] 找到 Alembic 配置，理解迁移文件的结构
- [ ] 执行一次数据库迁移，观察 SQL 输出
- [ ] 理解 select()、where()、join() 等查询构建方式
- [ ] 找到分页查询的实现，理解 offset/limit 的用法

### 底层知识打卡
- [ ] **PostgreSQL**：事务（Transaction）的 ACID 特性；索引的作用和类型
- [ ] **SQLAlchemy**：ORM vs 原生 SQL 的权衡；lazy loading vs eager loading
- [ ] **异步编程**：async/await 的工作原理；事件循环（Event Loop）
- [ ] **数据库迁移**：为什么需要迁移工具？直接改表结构有什么风险？

### 理解验证
1. SQLAlchemy 的 ORM 模式和 Core 模式有什么区别？项目用的是哪种？
2. 为什么要用异步 Session？同步 Session 有什么问题？
3. N+1 查询问题是什么？如何避免？
4. Alembic 迁移文件中的 upgrade() 和 downgrade() 分别做什么？

---

## Day 6：Redis 缓存集成

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 理解项目中 Redis 的使用场景和缓存策略
- 能读懂缓存相关代码，理解缓存键设计
- 理解 Redis 在会话管理、限流等场景的应用

### 操作 Checklist
- [ ] 找到 Redis 连接配置和连接池初始化代码
- [ ] 找到所有使用 Redis 的位置，分类整理使用场景
- [ ] 理解缓存键（key）的命名规范
- [ ] 找到缓存失效（TTL）的设置，理解过期策略
- [ ] 进入 Redis 容器，用 redis-cli 查看实际存储的数据
- [ ] 理解 JWT Token 黑名单的实现方式
- [ ] 找到限流（Rate Limiting）的实现（如果有）
- [ ] 理解缓存穿透、缓存击穿、缓存雪崩的防护措施

### 底层知识打卡
- [ ] **Redis**：五种基本数据结构（String/Hash/List/Set/ZSet）及使用场景
- [ ] **Redis**：持久化方式（RDB vs AOF）；连接池的必要性
- [ ] **缓存策略**：Cache-Aside、Write-Through、Write-Behind 的区别
- [ ] **分布式**：Redis 在分布式系统中的角色；分布式锁的基本原理

### 理解验证
1. 项目中 Redis 主要用于哪些场景？为什么选择 Redis 而不是内存缓存？
2. 缓存穿透和缓存击穿有什么区别？各自如何防护？
3. JWT Token 为什么需要黑名单？Redis 为什么适合存储黑名单？
4. Redis 的 EXPIRE 命令和 TTL 命令分别做什么？

---

## Day 7：中间件 + 异常处理 + 日志

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 理解 FastAPI 中间件的工作机制和执行顺序
- 能读懂项目中的全局异常处理器
- 理解结构化日志的配置和使用

### 操作 Checklist
- [ ] 找到所有中间件的注册位置，理解执行顺序
- [ ] 阅读 CORS 中间件的配置，理解跨域问题
- [ ] 找到全局异常处理器（exception_handler），阅读其实现
- [ ] 理解自定义异常类的设计（业务异常 vs 系统异常）
- [ ] 找到日志配置文件，理解日志级别和输出格式
- [ ] 触发一个业务异常，观察响应格式和日志输出
- [ ] 理解请求 ID（Request ID）的生成和传递
- [ ] 找到访问日志（Access Log）的记录方式

### 底层知识打卡
- [ ] **FastAPI**：中间件的洋葱模型；请求/响应的生命周期
- [ ] **Python**：异常继承体系；try/except/finally 的执行顺序
- [ ] **日志**：结构化日志 vs 非结构化日志；日志级别的语义
- [ ] **Linux**：tail -f 实时查看日志；grep 过滤日志内容

### 理解验证
1. 中间件和路由装饰器有什么区别？什么情况下用中间件？
2. FastAPI 的异常处理器和 Python 的 try/except 有什么关系？
3. 为什么要给每个请求分配唯一的 Request ID？
4. 日志级别 DEBUG/INFO/WARNING/ERROR/CRITICAL 各自适用什么场景？

---

## Day 8：认证与权限（JWT + RBAC）

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 深入理解 JWT 认证的完整流程
- 理解 RBAC（基于角色的访问控制）的实现
- 能追踪一个需要权限的接口的完整鉴权链路

### 操作 Checklist
- [ ] 找到 JWT Token 的生成代码，理解 payload 结构
- [ ] 找到 Token 验证的依赖函数，理解验证流程
- [ ] 阅读 RBAC 相关的数据模型（用户/角色/权限）
- [ ] 找到权限校验的实现，理解权限码的设计
- [ ] 用 Swagger UI 完成登录 → 获取 Token → 调用需权限接口的完整流程
- [ ] 理解 Access Token 和 Refresh Token 的区别和使用场景
- [ ] 找到 Token 刷新接口，理解刷新机制
- [ ] 理解 Token 黑名单（登出）的实现

### 底层知识打卡
- [ ] **JWT**：Header/Payload/Signature 三部分的作用；签名算法（HS256 vs RS256）
- [ ] **RBAC**：用户-角色-权限三层模型；权限粒度的设计
- [ ] **安全**：HTTPS 的必要性；Token 存储的安全考量
- [ ] **Redis**：Token 黑名单的存储和查询效率

### 理解验证
1. JWT 是无状态的，那么如何实现登出（Token 失效）？
2. Access Token 为什么要设置较短的过期时间？
3. RBAC 中，如果一个用户有多个角色，权限如何合并？
4. 为什么不能把敏感信息（如密码）放在 JWT Payload 中？

---

## Day 9：Celery 异步任务

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 理解 Celery 的架构和工作原理
- 能读懂项目中的任务定义和调用方式
- 理解消息队列（RabbitMQ）在异步任务中的角色

### 操作 Checklist
- [ ] 找到 Celery 配置文件，理解 broker 和 backend 的配置
- [ ] 找到所有 @celery_app.task 的定义，理解任务类型
- [ ] 找到任务调用的位置（.delay() 或 .apply_async()）
- [ ] 访问 Flower（http://localhost:5555），观察任务执行状态
- [ ] 手动触发一个异步任务，在 Flower 中追踪执行过程
- [ ] 找到 Celery Beat 的定时任务配置
- [ ] 理解任务重试（retry）的配置和使用场景
- [ ] 进入 RabbitMQ 管理界面，观察 Queue 和 Exchange

### 底层知识打卡
- [ ] **RabbitMQ**：Exchange 类型（direct/fanout/topic）；Queue 和 Binding 的关系
- [ ] **Celery**：Worker 的并发模型（prefork/gevent/eventlet）；任务序列化
- [ ] **消息队列**：为什么需要消息队列？同步调用有什么问题？
- [ ] **分布式**：任务幂等性的重要性；at-least-once vs exactly-once 语义

### 理解验证
1. Celery 的 broker 和 backend 分别是什么？可以用不同的服务吗？
2. .delay() 和 .apply_async() 有什么区别？
3. 如果 Worker 在执行任务时崩溃，任务会丢失吗？
4. 什么是任务幂等性？为什么异步任务必须考虑幂等性？

---

## Day 10-11：AI 接口预演 — 流式输出接口

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 理解 SSE（Server-Sent Events）和流式响应的实现原理
- 能在 FastAPI 中实现一个流式输出接口
- 理解 AI 大模型 API 的调用方式和流式处理

### 操作 Checklist（Day 10）
- [ ] 理解 HTTP 流式响应（Streaming Response）的工作原理
- [ ] 找到项目中 StreamingResponse 的使用（如果有）
- [ ] 阅读 FastAPI 官方文档中 StreamingResponse 的示例
- [ ] 实现一个简单的 SSE 接口，每秒推送一条消息
- [ ] 用浏览器或 curl 测试 SSE 接口，观察流式输出效果
- [ ] 理解 Content-Type: text/event-stream 的格式规范

### 操作 Checklist（Day 11）
- [ ] 注册一个 AI 大模型 API（OpenAI/Claude/通义千问等）
- [ ] 阅读所选 API 的流式调用文档
- [ ] 实现一个 FastAPI 接口，调用 AI API 并流式转发响应
- [ ] 处理流式响应中的错误和超时
- [ ] 实现请求取消（客户端断开连接时停止生成）
- [ ] 测试并发流式请求的性能表现
- [ ] 理解 Token 计费和流式响应的关系

### 底层知识打卡
- [ ] **HTTP**：长连接 vs 短连接；Keep-Alive 的作用
- [ ] **SSE**：SSE vs WebSocket 的适用场景；SSE 的重连机制
- [ ] **异步编程**：异步生成器（async generator）的工作原理
- [ ] **AI API**：大模型的 Token 概念；流式输出的 chunk 格式

### 理解验证
1. SSE 和 WebSocket 有什么区别？流式 AI 输出为什么通常用 SSE？
2. FastAPI 的 StreamingResponse 和普通 Response 有什么区别？
3. 如果客户端在流式输出中途断开连接，服务端如何感知并停止生成？
4. 异步生成器（async def + yield）在流式接口中起什么作用？

---

## Day 12：回顾 + 自建完整模块

**完成状态**
- [ ] 未开始
- [ ] 进行中
- [ ] 已完成

### 今日目标
- 系统回顾 12 天所学内容，查漏补缺
- 独立实现一个完整的业务模块（从 Model 到 API）
- 能用自己的话讲清楚整个项目的架构

### 操作 Checklist
- [ ] 回顾每天的理解验证问题，重新作答
- [ ] 画出完整的项目架构图（包含所有服务和数据流）
- [ ] 选择一个新的业务场景（如：文章管理、订单系统）
- [ ] 独立完成 Model 定义（SQLAlchemy）
- [ ] 独立完成 Schema 定义（Pydantic）
- [ ] 独立完成 CRUD 层实现
- [ ] 独立完成 Service 层实现
- [ ] 独立完成 Router 层实现（含权限控制）
- [ ] 为新模块添加 Celery 异步任务（如：发送通知）
- [ ] 为新模块添加 Redis 缓存
- [ ] 编写接口测试，验证所有功能正常
- [ ] 整理学习笔记，总结踩坑经验

### 底层知识打卡
- [ ] **架构**：能解释为什么项目要这样分层？每层的边界在哪里？
- [ ] **工程**：代码复用的原则；什么时候抽象，什么时候保持简单
- [ ] **性能**：识别项目中的性能瓶颈；N+1 查询、缓存缺失等问题
- [ ] **安全**：SQL 注入防护；XSS/CSRF 防护；敏感数据处理

### 理解验证
1. 不看代码，能否从头描述一个 POST 请求的完整处理流程？
2. 项目中哪些设计决策让你印象深刻？为什么这样设计？
3. 如果让你重新设计这个项目，你会改变什么？
4. 你遇到的最难的问题是什么？最终如何解决的？

---

## 总体进度统计

| 天数 | 主题 | 状态 | 完成日期 |
|------|------|------|----------|
| Day 1 | 环境拉起 + 项目全貌 | [ ] 未开始 | |
| Day 2 | 项目结构与路由分发 | [ ] 未开始 | |
| Day 3 | Pydantic 数据校验 + 响应模型 | [ ] 未开始 | |
| Day 4 | 依赖注入（Depends） | [ ] 未开始 | |
| Day 5 | SQLAlchemy ORM + CRUD 层 | [ ] 未开始 | |
| Day 6 | Redis 缓存集成 | [ ] 未开始 | |
| Day 7 | 中间件 + 异常处理 + 日志 | [ ] 未开始 | |
| Day 8 | 认证与权限（JWT + RBAC） | [ ] 未开始 | |
| Day 9 | Celery 异步任务 | [ ] 未开始 | |
| Day 10-11 | AI 接口预演 — 流式输出接口 | [ ] 未开始 | |
| Day 12 | 回顾 + 自建完整模块 | [ ] 未开始 | |

---

*最后更新：___________*
